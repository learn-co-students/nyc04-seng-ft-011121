<h1>All the Courses</h1>

<ul id="courses">
    <% @courses.each do |course|  %>
        <li class="course">
            <img class="image" src="<%= course.picture %>" alt="<%= course.name %>" />
            <%= link_to course.name, course_path(course) %>
        </li>
    <% end %>
</ul>